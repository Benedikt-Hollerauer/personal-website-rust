name: CI and Deploy

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  deploy:
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    steps:
      - name: Setup SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          ssh-keyscan -p ${{ secrets.SSH_PORT }} ${{ secrets.SSH_HOST }} >> ~/.ssh/known_hosts
      - name: Deploy
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ secrets.SSH_PORT }}
          envs: SUDO_PASSWORD # Pass the environment variable to the SSH session
          command_timeout: 20m
          script: |
            set -e
            
            # This works now because of your visudo change
            sudo chown -R $USER:$USER /srv/personal-website-rust
            
            git config --global --add safe.directory /srv/personal-website-rust
            cd /srv/personal-website-rust
            
            # Fetch and Force Reset
            git fetch origin main
            git checkout main
            git reset --hard origin/main
            git clean -fd
            
            # Capture the hash for the mailserver check
            PREV_HEAD=$(git rev-parse HEAD)
            
            # Build and Deploy
            bash scripts/init-env.sh
            docker compose up -d --build
            docker compose restart caddy
            
            # Conditional restart for mailserver
            if git diff --name-only "$PREV_HEAD" HEAD | grep -q "^mailserver-config/"; then
              docker compose restart mailserver
            fi